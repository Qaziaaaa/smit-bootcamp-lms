// Storage keys — used by AuthContext and apiClient to store/retrieve token and user.
// Using consistent keys prevents typos and makes them easy to change later.
export const TOKEN_KEY = 'lms_token'
export const USER_KEY = 'lms_user'
